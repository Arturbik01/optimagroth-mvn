spring:
  application:
    name: config-server
  profiles:
    active:
      - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/Arturbik01/config.git
          default-label: main
#          searchPaths: licensingservice

server:
  port: 8071

#Используется ли если запущено в контейнере?
#encrypt:
#  key: san

#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"
